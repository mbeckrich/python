def plusOne(number):  # defines function with parameter variable `number`
    return number + 1  # returns value of number's argument + 1 when called


newNumber = plusOne(
    5
)  # argument of 5 is passed to number + 1 and turned into variable newNumber
print(newNumber)  # prints variable newNumber

# Every function has a return value. The default value is None.
