# A simple way to count up to but not including 100
print(list(range(0, 100, 2)))  # starts at 0, goes to 98
# increasing by 2 (the step) each time

spam = list(range(0, 100, 2))  # assigns list to variable
print(spam)
