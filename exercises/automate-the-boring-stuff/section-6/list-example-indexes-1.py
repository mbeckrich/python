spam = ["cat", "bat", "rat", "elephant"]
spam[0]  # this uses the index to find the place of 0 in the list
# `spam` shows ['cat', 'bat', 'rat', 'elephant'][0]
# and [0] matches to the place of the first item, `cat`
